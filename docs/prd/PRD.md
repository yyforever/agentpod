# AgentPod 产品需求文档（PRD）

> 版本：v0.1 | 日期：2026-02-15
> 汇总自：01-用户画像 / 02-痛点验证 / 03-竞品分析 / 04-工程设计

---

## 一、定位与假设

### 一句话定位

**AgentPod 是面向 SaaS 开发者的 AI Agent 多租户编排平台，让开发者用一条命令为每个客户部署独立的数字员工实例。**

### 品类定义

| 维度 | 说明 |
|------|------|
| **是** | Agent-Aware 的多租户容器编排层 |
| **不是** | 通用 PaaS（Coolify）、AI 工作流平台（Dify）、白标 SaaS（Stammer.ai）、Kubernetes |
| **类比** | "Vercel for AI Agents" —— 开发者推配置，平台自动部署、路由、监控 |

### 市场判断

1. **AI Agent 市场正在从"构建"转向"运营"** —— 构建 Agent 的工具（Dify、LangChain）已成熟，但部署和管理多个 Agent 实例的工具严重缺失
2. **OpenClaw 是新一代 Agent 的标杆** —— 180K+ Stars，真正自托管、多通道、有记忆系统的 AI 助手，但其多租户部署体验极差
3. **SaaS 开发者需要"最后一公里"工具** —— 从"我做了一个 Agent"到"为 50 个客户运行 50 个独立实例"之间缺少桥梁

---

## 二、目标用户

### 核心用户：SaaS 开发者 / AI 解决方案商

为企业客户部署产品化 AI Agent（数字员工）的技术团队。

### 三个典型 Persona

| Persona | 角色 | 团队 | 客户规模 | 核心痛点 |
|---------|------|------|----------|----------|
| **赵明** | AI 客服 SaaS 创始人 | 3-5 人 | 10-50 家 | 40% 时间花在运维，手动部署 30min/客户 |
| **李娜** | 数字化服务商技术总监 | 8-15 人 | 5-20 家（每家 2-5 Agent） | 60 个容器逐个升级，多 LLM 配置混乱 |
| **王鹏** | 独立开发者/AI 代理商 | 1 人 | 3-10 个 | 4GB VPS 上端口/内存冲突，没时间写脚本 |

### 不服务的用户

- 个人 AI 玩家（单实例 Docker Compose 足够）
- 大型企业 IT 部门（有能力自建 K8s）
- 非技术代理商（需要零代码白标平台）

> 详见 [01-user-personas-and-scenarios.md](./01-user-personas-and-scenarios.md)

---

## 三、核心场景

### P0 场景（Must Have）

| 场景 | 当前痛点 | AgentPod 解法 | 验收标准 |
|------|---------|--------------|----------|
| **S1: 创建 Agent 租户** | 12 步手动操作，30-60 分钟 | `agentpod create <tenant>` 一条命令 | 端口/Volume/Traefik/证书/Token 全部自动化，< 3 分钟 |
| **S2: 查看运行状态** | `docker ps` + `docker logs` 逐个查看 | Dashboard 统一视图 + CLI `agentpod list` | 显示所有租户的状态/健康/资源用量 |
| **S3: 故障自动恢复** | 依赖人工发现，客户反馈才知道 | Reconciliation Loop 每 30s 检测 + 自动恢复 | 容器崩溃后 30s 内自动拉起；连续 3 次失败触发告警 |

### P1 场景（Should Have）

| 场景 | 当前痛点 | AgentPod 解法 |
|------|---------|--------------|
| **S4: 批量升级版本** | 逐个停止/删除/重建 | `agentpod upgrade --all` 滚动升级 |
| **S5: 按客户配置 LLM 和通道** | 每个 openclaw.json 手动编辑 | Dashboard 租户配置页 + 加密存储 |
| **S6: 资源限制** | 无限制，noisy neighbor 风险 | 创建时自动设置 CPU/内存上限 |

### P2 场景（Could Have）

| 场景 | 说明 |
|------|------|
| S7: 客户级 API 用量统计 | 追踪每个租户的 LLM API 调用量和成本 |
| S8: 备份与恢复 | Volume 快照 + 一键恢复 |

> 详见 [01-user-personas-and-scenarios.md](./01-user-personas-and-scenarios.md)

---

## 四、痛点优先级（ICE 评分）

| 排名 | 痛点 | ICE 分 | MVP 纳入 |
|------|------|--------|----------|
| 1 | 端口分配冲突 | 640 | ✅ Must |
| 2 | 状态目录隔离 | 640 | ✅ Must |
| 3 | 手动 Docker 编排 | 630 | ✅ Must |
| 4 | 资源限制缺失 | 504 | ✅ Should |
| 5 | 无自动故障恢复 | 504 | ✅ Must |
| 6 | Token 管理 | 500 | ✅ Must |
| 7 | 无统一监控视图 | 384 | ✅ Must |
| 8 | HTTPS 证书管理 | 336 | ✅ Should |
| 9 | 配置管理混乱 | 245 | ✅ Should |
| 10 | 版本升级困难 | 210 | ⬜ Could |
| 11 | Bridge 网络死锁 | 150 | ✅ Must（前置 PoC） |

**最小可用门槛**：解决排名 1-3、5、7、11 才有人用（自动化编排 + 故障恢复 + 统一监控 + 网络可行性）。

> 详见 [02-pain-points-validation.md](./02-pain-points-validation.md)

---

## 五、差异化定位

### 为什么不用 X？

| 替代方案 | AgentPod 的优势 |
|----------|-----------------|
| **Docker Compose + Traefik** | AgentPod = 手动方案 + 状态管理 + 自动化 + 监控。5 租户内手动方案够用，10+ 租户 AgentPod 价值显现 |
| **Coolify** | Coolify 不支持端口范围映射、不理解 openclaw.json、WebSocket 有已知问题 |
| **Kubernetes** | K8s 学习曲线 6-12 月、最低 3 节点、需要专职 DevOps —— 对 3-15 人团队是大炮打蚊子 |
| **白标平台** | 白标平台是 SaaS 托管模式，AgentPod 是自托管模式。选 AgentPod 的理由：数据控制权 + OpenClaw 完整能力 |

### 核心差异

**Coolify 知道你跑了一个容器；AgentPod 知道你跑了一个会说话的数字员工。**

Agent-Aware 意味着：
- 自动计算端口派生（base → base+2, base+9~108）
- 根据租户参数生成 openclaw.json
- 通过 WebSocket heartbeat 检查 Gateway 健康（而非简单 HTTP 200）
- 理解通道状态（WhatsApp session、Telegram bot token）

> 详见 [03-competitive-analysis.md](./03-competitive-analysis.md)

---

## 六、技术架构

### 三层架构

```
┌─────────────────────────────────────────┐
│ Layer 1: Dashboard（Next.js 15）          │
│ 管理界面：租户列表、创建、配置、监控       │
│ NextAuth v5 单管理员认证                  │
│ SSE 实时状态更新                          │
├─────────────────────────────────────────┤
│ Layer 2: Control Plane（Hono API）        │
│ 独立 Node.js 进程                        │
│ REST API（租户 CRUD、配置管理）            │
│ Reconciliation Engine（每 30s 调和）      │
│ 健康检查器                               │
│ PostgreSQL 状态存储                       │
├─────────────────────────────────────────┤
│ Layer 3: Data Plane                      │
│ Traefik v3（反向代理，Docker 自动发现）    │
│ Agent 容器（Pod:acme, Pod:beta, ...）     │
│ 每租户独立 Docker Volume                  │
│ Bridge 网络：agentpod-net                │
└─────────────────────────────────────────┘
```

### 四层代码架构（闭环工程）

```
Harness（CLI）→ Shell（Dashboard/API）→ Core（纯逻辑）→ Services（外部依赖）
```

- **Harness**：`agentpod create/status/health/doctor` —— Agent 直接验证的入口
- **Shell**：Dashboard + REST API —— 仅做 I/O
- **Core**：Reconciler / TenantManager / PortAllocator —— 100% 可测试的纯逻辑
- **Services**：Docker API / PostgreSQL / Traefik —— 通过 interface 隔离，可 mock

### CLI-First 原则

所有功能先实现 CLI 命令，再做 Dashboard UI：
- CLI 输出可 pipe、可 diff、可断言 → 天然闭环
- AI Agent 友好（零上下文成本）
- Dashboard 是 CLI 的 GUI 映射，不是独立功能层

> 详见 [04-engineering-design.md](./04-engineering-design.md)

---

## 七、功能需求（MoSCoW）

### Must Have（MVP 必须）

| 功能 | 描述 | CLI 命令 |
|------|------|---------|
| **租户创建** | 自动分配端口、Volume、Traefik 路由、Token，启动 OpenClaw 容器 | `agentpod create <id>` |
| **租户删除** | 停止容器、清理 Volume、注销路由 | `agentpod delete <id>` |
| **租户列表** | 显示所有租户状态、健康、资源用量 | `agentpod list` |
| **租户详情** | 单个租户的完整信息（端口、配置、日志） | `agentpod status <id>` |
| **Reconciliation Loop** | 每 30s 对比期望/实际状态，自动创建/重启/清理容器 | `agentpod reconcile` |
| **健康检查** | HTTP + WebSocket heartbeat 双重检测 | `agentpod health` |
| **系统诊断** | 检查 Docker、PostgreSQL、Traefik 依赖是否就绪 | `agentpod doctor` |
| **Dashboard 基础** | 租户列表 + 创建表单 + 状态概览 | Web UI |
| **管理员认证** | 单管理员登录 | NextAuth v5 |

### Should Have（MVP 期望）

| 功能 | 描述 |
|------|------|
| **资源限制** | 创建租户时设置 CPU/内存上限 |
| **HTTPS 自动化** | Traefik ACME 自动签发 Let's Encrypt 证书 |
| **租户配置管理** | Dashboard 中编辑 LLM Provider、通道绑定等 |
| **事件日志** | 记录所有操作（create/delete/restart/error） |
| **告警通知** | 故障恢复失败时通过 Webhook 通知 |

### Could Have（后续迭代）

| 功能 | 描述 |
|------|------|
| **滚动升级** | `agentpod upgrade --all` 逐个升级 |
| **API 用量统计** | 按租户追踪 LLM API 调用量 |
| **备份/恢复** | Volume 快照 + 一键恢复 |
| **多节点** | SSH 到远程服务器管理容器 |

### Won't Have（MVP 不做）

| 功能 | 原因 |
|------|------|
| 多 Agent 类型支持 | MVP 只支持 OpenClaw |
| 内置计费系统 | 不是 AgentPod 的核心价值 |
| 白标定制 | 面向开发者，不需要终端用户品牌 |
| Kubernetes 支持 | 目标用户不用 K8s |

---

## 八、非功能需求

| 维度 | 要求 |
|------|------|
| **性能** | 单机支撑 50 个租户容器；Reconcile 周期 30s；Dashboard 首屏 < 2s |
| **可靠性** | 容器崩溃 30s 内自动恢复；控制面崩溃不影响已运行的 Agent 容器 |
| **安全** | PostgreSQL RLS 租户隔离；API Key 加密存储；Gateway Token 自动生成 |
| **资源** | Control Plane + Dashboard + Traefik + PostgreSQL 自身开销 < 1GB RAM |
| **部署** | `docker compose up -d` 一键启动全套 |
| **兼容** | Node.js 22+；Docker 24+；PostgreSQL 16+；Traefik v3 |

---

## 九、核心假设与验证

### 前置验证（编码前必须完成）

| ID | 假设 | 验证方法 | 失败应对 |
|----|------|----------|----------|
| **TH-1** | Traefik 能正确转发 WebSocket 到 Bridge 网络中的 OpenClaw | PoC 搭建 | 尝试 header 配置 → allowedOrigins → allowInsecureAuth → 重新评估架构 |

### 技术假设（开发中验证）

| ID | 假设 | 置信度 | 验证方法 |
|----|------|--------|----------|
| TH-2 | 4GB VPS 可运行 20 个 OpenClaw 容器 | 🟡 中 | 压测 |
| TH-3 | Reconciliation 30s 周期无性能瓶颈 | 🟢 高 | 单元测试 + 压测 |
| TH-4 | dockerode 可稳定管理 50+ 容器 | 🟢 高 | 集成测试 |
| TH-5 | PostgreSQL RLS 对 100 租户查询 < 10ms | 🟢 高 | 基准测试 |

### 产品假设（发布后验证）

| ID | 假设 | 置信度 | 验证方法 |
|----|------|--------|----------|
| PH-1 | 多实例 OpenClaw 部署是真实需求 | 🟡 中 | GitHub Stars + Issue |
| PH-2 | 开发者愿意从手动脚本迁移 | 🟡 中 | PR 参与度 |
| PH-3 | Dashboard 比纯 CLI 更有吸引力 | 🟡 中 | 使用数据 |

---

## 十、开发路线图

### Phase 0: PoC 验证（Week 0, 2 天）

- 验证 TH-1：WebSocket + Traefik + Bridge 网络
- **Gate**：通过 → Phase 1；失败 → 降级方案评估

### Phase 1: 核心引擎（Week 1-2）

- Monorepo 搭建（Turborepo + pnpm）
- Core 层：Reconciler + TenantManager + PortAllocator
- Services 层：Docker API + PostgreSQL schema
- Harness 层：`agentpod create/delete/status/health/doctor/reconcile`
- OpenClaw Adapter 实现
- **闭环验证**：`create → status → kill → auto-recover → delete` 全流程 CLI 通过

### Phase 2: Dashboard（Week 3-4）

- Next.js App Router + shadcn/ui + NextAuth v5
- 租户列表 / 创建 / 详情页
- SSE 实时状态更新
- **闭环验证**：CLI 创建 → Dashboard 实时显示；Dashboard 创建 → CLI 同步

### Phase 3: 稳定性与发布（Week 5-6）

- 健康检查（HTTP + WebSocket heartbeat）
- 资源监控（docker stats 聚合）
- 事件日志
- `docker compose` 一键部署
- README + 快速开始文档
- **闭环验证**：全新 VPS 上 `docker compose up` → 创建 3 个租户 → 模拟故障 → 自动恢复

---

## 十一、风险与缓解

| 风险 | 概率 | 影响 | 缓解策略 |
|------|------|------|----------|
| **TH-1 失败**：WebSocket + Bridge 网络不通 | 中 | 致命 | 降级方案 A/B/C/D（详见 02 文档） |
| **OpenClaw API 变更**：上游重构破坏 Adapter | 低 | 高 | Adapter interface 抽象；锁定 OpenClaw 版本 |
| **市场需求不足**：没有足够多的多租户场景用户 | 中 | 高 | MVP 快速验证（6 周）；Core 层可复用于其他 Agent |
| **单机瓶颈**：50+ 容器超出单 VPS 能力 | 低 | 中 | Phase 2 后评估多节点方案 |
| **安全漏洞**：容器逃逸或跨租户数据泄漏 | 低 | 致命 | RLS + 网络隔离 + 最小权限 + 安全审计 |

### 退路（Plan B）

如果 AgentPod 整体方向验证失败（PH-1 不成立），Core 层的以下组件可独立复用：
- Reconciliation Engine → 通用 Docker 容器编排库
- Port Allocator → Docker 端口管理工具
- OpenClaw Adapter → OpenClaw 部署自动化脚本

---

## 十二、成功标准

### M3（3 个月）

| 指标 | 目标 |
|------|------|
| GitHub Stars | 500+ |
| 活跃部署 | 20+ |
| 管理的租户总数 | 100+ |
| 核心功能 | create / delete / monitor / auto-recover |
| 文档 | 快速开始 + API 文档 |

### M12（12 个月）

| 指标 | 目标 |
|------|------|
| GitHub Stars | 3,000+ |
| 活跃部署 | 200+ |
| 管理的租户总数 | 2,000+ |
| Agent 类型 | 3+（OpenClaw + 其他） |
| 社区贡献者 | 10+ |

---

## 附录：文档索引

| 文档 | 内容 |
|------|------|
| [01-user-personas-and-scenarios.md](./01-user-personas-and-scenarios.md) | 用户画像、核心场景、用户旅程、假设清单 |
| [02-pain-points-validation.md](./02-pain-points-validation.md) | 11 个痛点验证、ICE 评分、最小可用门槛、风险评估 |
| [03-competitive-analysis.md](./03-competitive-analysis.md) | 竞品能力矩阵、"为什么不用 X"、差异化定位 |
| [04-engineering-design.md](./04-engineering-design.md) | 闭环工程方法论、CLI-First 设计、测试策略、CI 分层、交付契约 |
